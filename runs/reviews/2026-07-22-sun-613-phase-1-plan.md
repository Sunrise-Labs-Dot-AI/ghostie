# Plan review, SUN-613 phase 1 read-only (redaction projection)

Author lane: Claude (Opus 4.8). Second lane: Codex, independent. 2026-07-22.
**Verdict: BLOCK**, 7 CRITICAL, 5 WARNING. Plan rewritten; phase boundary moved.

## What the review changed (not just the field list)

Findings 7, 8, 9 together showed the phase itself was mis-scoped: writing per-draft snapshot FILES,
wired into DraftStore, before the phase-2 data plane (push vs pull) and queue schema (manifest,
generation, tombstones) are decided, is premature AND persists message bodies to disk for no runtime
consumer, which is the same dead-code problem the plan claimed to avoid. The reviewer's explicit
carve-out: if redaction must land first, limit phase 1 to a PURE projection with adversarial tests,
no wiring, no bodies on disk. That is what shipped.

So phase 1 became the transport-independent privacy core: the redaction projection and its tests.
Everything with a runtime footprint (publisher, storage, manifest, credential, flag, wiring) moved
to phase 2 as one vertical slice with its consumer.

## Findings and disposition

| # | Sev | Finding | Disposition |
|---|---|---|---|
| 1 | CRITICAL | Redaction was a nested denylist; copying `[ContextMessage]` leaks third-party handles, message ids, reaction authors | FIXED: dedicated `RelayContextMessage` allowlist; tests assert exact encoded key set of every type |
| 2 | CRITICAL | `to_handle_name ?? to_handle` leaks a group's guid or participant handles for unnamed groups | FIXED: `RelayRecipient.safeGroupLabel` emits a people-count on the no-names path, never handles/guid |
| 3 | WARNING | Remote PostHog flag treated as privacy consent | DEFERRED to phase 2: no flag in phase 1; phase 2 requires availability flag AND local opt-in before any body is copied |
| 4 | CRITICAL | Flag-off retraction guarantee false (flag change does not call applyRefresh; app-stopped case) | DEFERRED to phase 2 with the publisher; recorded as a phase-2 requirement (purge on flag transition + startup, read gated independently of deletion) |
| 5 | CRITICAL | Detached off-actor reconcile can resurrect a deleted draft; `[Draft]` crosses isolation without Sendable | DEFERRED to phase 2: serial publisher actor with monotonic generations. No wiring exists in phase 1 |
| 6 | WARNING | Partial draft load becomes an authoritative deletion | DEFERRED to phase 2: per-source completeness, delete-by-absence only after a complete scan |
| 7 | CRITICAL | Data plane (push vs pull) undecided; hub cannot read a spoke's local dir | ACCEPTED: phase 1 no longer builds storage; phase 2 decides the data plane first |
| 8 | CRITICAL | Schema cannot represent an authoritative queue (no generation, publish time, membership, tombstones); `snapshot(from:)` not pure if it reads identity | FIXED (purity: device id injected) + DEFERRED (manifest to phase 2) |
| 9 | CRITICAL | Phase 1 was the same dead code it rejected, and persisted bodies | FIXED: phase 1 writes nothing to disk; pure library + tests only |
| 10 | WARNING | Draft-id not validated before use as a filename; symlink/mode gaps; temp leak | DEFERRED to phase 2 with the file writer |
| 11 | WARNING | Republishing `sent` drafts turns the queue into a week of message history | DEFERRED to phase 2: publish only pending/scheduled/held, or a short relay TTL |
| 12 | WARNING | Untrusted text has no browser-rendering contract | PARTLY FIXED now: fields documented and typed as untrusted plain text, `textIsUntrusted` marker + adversarial script/bidi fixtures; phase 3 enforces textContent + CSP |

## What landed

`menubar/Sources/MessagesForAIMenu/RelaySnapshot.swift`: `RelaySnapshot`, `RelayRecipient`,
`RelayContextMessage`, `RelayLifecycle`, and the pure `project(from:originDeviceID:)`. No file I/O,
no identity read, no flag, no wiring.

`menubar/Tests/MessagesForAIMenuTests/RelaySnapshotTests.swift`: 14 tests. The load-bearing ones
assert the EXACT encoded key set of each projection type (so a future field cannot leak by default),
that no sensitive value from a fully-loaded hostile draft appears anywhere in the JSON, that an
unnamed group becomes a count, that an unknown context sender becomes a pseudonym, that the
projection creates no file, and that script/bidi-shaped text is carried inert.

## Phase-2 requirements carried forward

Recorded in `docs/plans/cross-device-draft-sync-spec.md` and the phase-1 plan: data-plane decision
before storage (7); per-origin manifest with generation/publish-time/membership/tombstones (6, 8);
serial publisher actor, generation-ordered, Sendable projections (5); flag-off purge on transition
and startup, reads gated independently of deletion (4); availability flag AND local opt-in before any
copy (3); publish only pending/scheduled/held or a short TTL (11); draft-id validation, platform
namespacing, O_NOFOLLOW, temp cleanup (10).

## Verification

- `cd menubar && swift build && swift test` -> 746 passed, 0 failed, 2 skipped (both pre-existing).
- The projection has no runtime footprint, so its validation IS the test suite.
