# Trusted relay v1 rollout, 2026-09-15

James approved building and shipping the trusted-relay model after reviewing its privacy boundary. This supersedes the earlier requirement to pass a relay-blind tunnel gate. HTTPS/WSS transport encryption remains required; the relay handles content in memory and intentionally retains no message bodies. The Mac still filters authentication material and enforces read/text-draft-only tools. No send authority is added. The app release follows local testing, with James running the final release command per the repo's runtime limitation.

## Implementation and acceptance
1. Inspect existing Clerk production instance, registered-client compatibility and Railway configuration without exposing secrets. Keep the existing app identity and login methods. Use an isolated real relay project, one replica and a private metadata volume. Production deployment is authorized by "build and ship"; do not change plans, weaken account security, or substitute development keys for production.
2. Add a minimal reproducible production container and startup configuration. Keep the Bun relay on loopback behind a same-container proxy, with HTTPS at Railway edge, no access/body/header logs, no traces, core dumps disabled, no proxy cache, bounded requests and WebSocket support. Verify all network legs, including edge-to-container transport, against Railway's deployment boundary; document limitations honestly.
3. Register explicit supported OAuth clients with exact published callback URIs. Exercise a real hosted client and a desktop client through Clerk sign-in, Mac pairing and per-Mac OAuth consent. If a supported client cannot use static registration, implement the minimum standards-based alternative only after its design is reviewed. Never bypass authorization for a live messaging test.
4. Add health/readiness and deploy-time checks that expose no account data. Verify synthetic message/credential canaries never enter application, proxy or platform logs or metadata SQLite. Test expiry, wrong account/client/resource, stop/quit/disconnect, timeout/no replay, malicious methods and filtering.
5. Set the app's default service address only after live service verification. Preserve opt-in and clear disclosure of relay visibility, heuristic filtering, AI-client access and offline behavior. Test the signed local app and synthetic draft review before release preparation.
6. Run relevant Bun typechecks/tests, full Swift build/tests, experience contracts, independent code review, PR and CI. Merge only after acceptance evidence. Verify authorized deployment after merge. Prepare the next release and run its dry-run; James executes actual notarization/release in his Terminal.

## Completion boundary
Shipping means an operational authenticated relay plus the configured app, verified live account/MCP flow and release artifacts. A fixture, unconfigured container, or green tests alone is insufficient. Record exact blockers if production keys, account-security handoff or James's release execution are required. Do all independent implementation, test and release preparation before handing off. No real messages in testing, logs or review artifacts; use synthetic fixtures until the intended app test is explicitly scoped.

## Plan review disposition
Claude Opus attempt 1 failed: OAuth session expired and could not be refreshed. Independent Codex gpt-5.5 identified three release blockers, all accepted: production artifacts, live OAuth acceptance, and deployment log evidence. These are implementation/acceptance work above; no real messaging release until proven. Use Railway standard HTTPS, same-container Caddy to loopback Bun, one process owner, no raw TCP proxy. The existing verified Clerk production domain is messagesfor.ai, so the dedicated service hostname is connect.messagesfor.ai. Railway documents automatic encryption from edge to applications at https://railway.com/features; TLS ends at the trusted edge and the app-facing container listener is HTTP. No end-to-end claim is made.
