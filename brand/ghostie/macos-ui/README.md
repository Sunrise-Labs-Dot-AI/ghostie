# Ghostie macOS UI Prototype

Prototype for the shared macOS app direction.

This is intentionally not a production SwiftUI change. It explores:

- macOS app icon treatment
- window wrapper
- grouped sidebar navigation
- general pages like Settings, Drafts, Scheduled, and History
- approval card language
- examples of tab-specific rooms inside the shared shell

Open `index.html` in a browser.
